 How to Run
1. Clone the repo.
2. Install requirements: `pip install -r requirements.txt`.
3. Run the engine: `python optimizer.py`.
4. Open `inventory_optimization_results.csv` to see the intelligence report.
